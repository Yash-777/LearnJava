/**
 * HTML sanitization is the process of examining an HTML document and producing a new HTML document
 * that preserves only whatever tags are designated "safe" and desired.
 * HTML sanitization can be used to protect against cross-site scripting (XSS)
 * attacks by sanitizing any HTML code submitted by a user.
 * https://ajax.googleapis.com/ajax/libs/angularjs/1.6.1/angular-sanitize.js
 */
